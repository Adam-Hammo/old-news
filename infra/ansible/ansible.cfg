[defaults]
inventory = inventory.ini
host_key_checking = False
roles_path = roles
# No stdout_callback: the `yaml` one lives in community.general now, and a whole
# collection is a lot to install for nicer-looking output.
interpreter_python = auto_silent

[ssh_connection]
pipelining = True
