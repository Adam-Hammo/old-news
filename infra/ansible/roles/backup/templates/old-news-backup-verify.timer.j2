[Unit]
Description=Monthly proof that the backups actually restore

[Timer]
# A restore pulls the whole repository back, which is the one thing here big enough to
# matter against Backblaze's free egress. Monthly is still a real test of it.
OnCalendar=monthly
RandomizedDelaySec=1h
Persistent=true

[Install]
WantedBy=timers.target
