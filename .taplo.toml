[formatting]
reorder_arrays = true
reorder_keys = false

# Sorting `dependencies` would leave each comment over the wrong package.
[[rule]]
keys = ["project"]

[rule.formatting]
reorder_arrays = false
