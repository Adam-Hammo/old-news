# Prettier owns wrapping and whitespace; markdownlint owns structure.
default: true
MD013: false # line length
MD024: { siblings_only: true } # repeated headings under different parents
MD033: false # inline HTML
MD041: false # first line need not be a heading
